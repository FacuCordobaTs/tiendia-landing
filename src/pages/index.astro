---
// index.astro - Landing page principal
import Layout from '../layouts/Layout.astro';
import Header from '../components/Header.astro';
import { Button } from "@/components/ui/button";
import { Card, CardContent, CardHeader, CardTitle } from "@/components/ui/card";
import { Badge } from "@/components/ui/badge";
import { Sparkles, Image as ImageIcon, Check, Clock, DollarSign, TrendingUp, Upload } from "lucide-react";

// Obtener el año actual para el footer
const currentYear = new Date().getFullYear();
---

<Layout title="Tiendia.app | Fotos de Producto con IA">
  <Header />
  <div class="min-h-screen bg-gradient-to-b from-background to-blue-50/50 dark:to-slate-900/50 flex flex-col items-center text-foreground px-4">
    
    <!-- Hero Section -->
    <section class="container pt-20 pb-8 md:pt-28 md:pb-24 text-center space-y-6 max-w-4xl">
      <Badge variant="outline" className="text-sm px-4 py-1 border-primary/50 bg-primary/10 text-primary font-medium shadow-sm">
        <Sparkles className="h-3 w-3 mr-2" /> tiendia.app
      </Badge>

      <div class="flex items-center justify-center gap-2 text-sm text-muted-foreground">
        <div class="flex items-center gap-1.5 bg-muted/50 px-3 py-1.5 rounded-full">
          <TrendingUp className="h-4 w-4 text-primary" />
          <span>Unete a <span class="font-semibold text-foreground">+150 tiendas</span> creando imágenes profesionales</span>
        </div>
      </div>

      <h1 class="text-4xl md:text-5xl lg:text-6xl font-extrabold tracking-tight leading-tight md:leading-snug">
        Transforma tus Fotos <span class="bg-gradient-to-r from-blue-500 to-cyan-400 dark:from-blue-400 dark:to-cyan-300 bg-clip-text text-transparent">en Imagenes Profesionales</span>. ¡Desde $150 pesos por imagen!
      </h1>

      <p class="text-lg md:text-xl text-muted-foreground max-w-2xl mx-auto">
        Sube la foto de tu producto y deja que nuestra IA genere imágenes profesionales de alta calidad en segundos. ¡Sin necesidad de estudio ni fotógrafo!
      </p>

      <div class="flex flex-col sm:flex-row gap-4 justify-center pt-4">
         <a href="https://my.tiendia.app/home">
          <Button 
         size="lg"
          className="gap-2 text-lg shadow-lg hover:shadow-primary/30 transition-shadow duration-300"
          >
           <Upload className="h-5 w-5" />
           Comenzar ahora
         </Button>
         </a>
      </div>
    </section>

    <!-- About Us Section -->
    <section class="container pb-16 md:py-24">
      <div class="max-w-4xl mx-auto">
        <div class="flex flex-col md:flex-row items-center gap-8 md:gap-12">
          <div class="w-48 h-48 md:w-64 md:h-64 rounded-full overflow-hidden border-4 border-primary/20 shadow-xl">
            <img
              src="/me.jpg"
              alt="Facundo Cordoba - Creador de tiendia.app"
              class="w-full h-full object-cover"
            />
          </div>
          <div class="flex-1 text-center md:text-left">
            <h2 class="text-3xl md:text-4xl font-bold mb-4 flex items-center justify-center md:justify-start gap-2">
              <span>👋</span> Sobre mi
            </h2>
            <p class="text-lg text-muted-foreground mb-4">
              ¡Hola! Soy Facundo Cordoba, estudiante de la UTN y creador de tiendia.app ✨
            </p>
            <p class="text-muted-foreground mb-4">
              Creé esta aplicación con un objetivo simple: ayudar a las tiendas de ropa a tener imágenes profesionales de sus productos a un precio accesible. Como estudiante, entiendo la importancia de mantener los costos bajos mientras se busca la mejor calidad posible 🎓
            </p>
            <p class="text-muted-foreground">
              Cada imagen que generas no solo mejora tu tienda, sino que también me ayuda a seguir mejorando la aplicación para ofrecerte mejores resultados. ¡Gracias por ser parte de este proyecto! 🤝
            </p>
          </div>
        </div>
      </div>
    </section>

    <!-- Before & After Section -->
    <section class="container py-16 md:py-24">
      <h2 class="text-3xl md:text-4xl font-bold text-center mb-12">
        De Esto... <span class="text-muted-foreground/80">a Esto</span> ✨
      </h2>
      <div class="grid md:grid-cols-2 gap-8 items-center max-w-5xl mx-auto">
        <!-- Antes -->
        <div class="flex flex-col items-center">
          <h3 class="text-xl font-semibold mb-4 text-muted-foreground">Tu Foto Original</h3>
          <Card className="overflow-hidden shadow-lg w-full">
            <img
              src="/placeholder-antes.jpeg" 
              alt="Foto de producto antes de la IA"
              class="aspect-square object-cover w-full transition-transform duration-300 ease-in-out hover:scale-105"
            />
          </Card>
        </div>

        <!-- Después -->
        <div class="flex flex-col items-center">
          <h3 class="text-xl font-semibold mb-4 text-primary">Generada con tiendia.app</h3>
          <Card className="overflow-hidden shadow-xl border-2 border-primary/50 w-full">
             <img
            src="/placeholder-despues.png" 
            alt="Foto de producto generada por IA"
            class="aspect-square object-cover w-full object-top transition-transform duration-300 ease-in-out hover:scale-105"
            />
          </Card>
        </div>
      </div>
      <p class="text-center text-muted-foreground mt-8 max-w-2xl mx-auto">
        Nuestra IA analiza tu producto y lo presenta en el mejor escenario posible, con iluminación y estilo profesional.
      </p>
    </section>

    <!-- Pricing Section -->
    <section class="container py-16 md:py-24">
      <h2 class="text-3xl md:text-4xl font-bold text-center mb-4">
        Packs de Imágenes
      </h2>
      <p class="text-center text-muted-foreground mb-12 flex items-center justify-center gap-2">
        🇦🇷 Precios en Pesos Argentinos
      </p>
      <div class="grid sm:grid-cols-2 lg:grid-cols-4 gap-6 max-w-6xl mx-auto">
        <!-- Single Image Pack -->
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="text-center">
            <CardTitle className="text-2xl font-bold">1 Imagen</CardTitle>
            <p class="text-3xl font-bold text-primary mt-2">$150</p>
          </CardHeader>
          <CardContent className="text-center">
            <p class="text-sm text-muted-foreground">Ideal para probar el servicio</p>
          </CardContent>
        </Card>

        <!-- 10 Images Pack -->
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="text-center">
            <CardTitle className="text-2xl font-bold">10 Imágenes</CardTitle>
            <p class="text-3xl font-bold text-primary mt-2">$1500</p>
          </CardHeader>
          <CardContent className="text-center">
            <p class="text-sm text-muted-foreground">Perfecto para pequeños catálogos</p>
          </CardContent>
        </Card>

        <!-- 50 Images Pack -->
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1 relative">
          <div class="absolute -top-3 left-1/2 -translate-x-1/2 bg-gradient-to-r from-primary to-primary/90 text-white dark:text-black px-4 py-1 rounded-full text-xs font-medium shadow-lg">
            Más elegido
          </div>
          <CardHeader className="text-center">
            <CardTitle className="text-2xl font-bold">50 Imágenes</CardTitle>
            <p class="text-3xl font-bold text-primary mt-2">$6600</p>
            <span class="text-sm text-green-600 dark:text-green-400">12.5% de ahorro</span>
          </CardHeader>
          <CardContent className="text-center">
            <p class="text-sm text-muted-foreground">Ideal para tiendas medianas</p>
          </CardContent>
        </Card>

        <!-- 100 Images Pack -->
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="text-center">
            <CardTitle className="text-2xl font-bold">100 Imágenes</CardTitle>
            <p class="text-3xl font-bold text-primary mt-2">$12,750</p>
            <span class="text-sm text-green-600 dark:text-green-400">15% de ahorro</span>
          </CardHeader>
          <CardContent className="text-center">
            <p class="text-sm text-muted-foreground">Para tiendas con gran catálogo</p>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- Features/Benefits Section -->
    <section class="container py-16 md:py-24 bg-muted/30 dark:bg-slate-800/20 rounded-2xl my-16">
      <h2 class="text-3xl md:text-4xl font-bold text-center mb-12">
        Ventajas que Impulsarán tu Tienda
      </h2>
      <div class="grid sm:grid-cols-2 lg:grid-cols-3 gap-6 max-w-6xl mx-auto">
        <!-- Feature Cards -->
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <ImageIcon className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Calidad de Estudio</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Obtén imágenes profesionales sin costosos equipos ni fotógrafos.</p>
          </CardContent>
        </Card>
        
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <Clock className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Generación Instantánea</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Transforma tus fotos en segundos, no días. Acelera tus lanzamientos.</p>
          </CardContent>
        </Card>
        
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <DollarSign className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Ahorro Significativo</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Reduce drásticamente los costos asociados a la fotografía de producto tradicional.</p>
          </CardContent>
        </Card>
        
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <TrendingUp className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Atrae Más Clientes</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Imágenes atractivas aumentan la confianza y las conversiones en tu tienda.</p>
          </CardContent>
        </Card>
        
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <Upload className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Increíblemente Fácil</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Solo necesitas subir tu foto. Nuestra IA se encarga de la magia.</p>
          </CardContent>
        </Card>
        
        <Card className="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1">
          <CardHeader className="flex flex-row items-center gap-4 pb-2">
            <div class="p-2 bg-primary/10 rounded-md">
              <Check className="h-6 w-6 text-primary" />
            </div>
            <CardTitle className="text-lg font-semibold">Resultados Consistentes</CardTitle>
          </CardHeader>
          <CardContent>
            <p class="text-sm text-muted-foreground">Mantén un estilo visual profesional y coherente en todos tus productos.</p>
          </CardContent>
        </Card>
      </div>
    </section>

    <!-- CTA Section -->
    <section class="container py-16 md:py-24 text-center space-y-6 max-w-3xl">
      <h2 class="text-3xl md:text-4xl font-bold">
        ¿Listo para Elevar tus Productos?
      </h2>
      <p class="text-lg md:text-xl text-muted-foreground">
        Regístrate y comienza a generar imágenes profesionales para tu tienda.
      </p>
      <a href="https://my.tiendia.app/home">
      <Button 
      size="lg" className="gap-2 text-lg shadow-lg hover:shadow-primary/30 transition-shadow duration-300">
          <Sparkles className="h-5 w-5" />
          Comenzar ahora
      </Button>
    </a>
    </section>

    <!-- Footer -->
    <footer class="container py-8 text-center text-sm text-muted-foreground mt-12 border-t border-border/20">
      © {currentYear} tiendia.app - Imágenes IA para tu Éxito. Todos los derechos reservados.
    </footer>
  </div>
</Layout>

<script>
  // Client-side script for interactive elements
  document.addEventListener('DOMContentLoaded', () => {
    // Add hover effects or any client-side functionality here
    const cards = document.querySelectorAll('.hover\\:-translate-y-1');
    cards.forEach(card => {
      card.addEventListener('mouseenter', () => {
        card.classList.add('-translate-y-1');
      });
      card.addEventListener('mouseleave', () => {
        card.classList.remove('-translate-y-1');
      });
    });
  });
</script>