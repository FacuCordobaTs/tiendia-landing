---
const { icon: Icon, title, description } = Astro.props;
---

<div class="bg-card/50 hover:border-primary/30 transition-colors duration-300 ease-in-out transform hover:-translate-y-1 border border-border rounded-lg p-6">
  <div class="flex flex-row items-center gap-4 pb-2">
    <div class="p-2 bg-primary/10 rounded-md">
      <Icon class="h-6 w-6 text-primary" />
    </div>
    <h3 class="text-lg font-semibold">{title}</h3>
  </div>
  <p class="text-sm text-muted-foreground">{description}</p>
</div>